# Model Layout

Place detector model assets under this package so runtime paths stay local to `oak_detectors`.

Suggested layout:

```text
models/
├── money/
└── navigation/
```

Examples of files that may live here:
- `*.blob`
- `*.onnx`
- `*.json`
- label maps or calibration metadata
