#!/usr/bin/env bash
# ตัวรัน node web_display: เรียกฟังก์ชัน main() ในโมดูล Python ตรง ๆ
# ใช้ bash wrapper แทน console_scripts เพื่อเลี่ยงปัญหา shebang ผิด python (ดู HANDOFF.md)
exec python3 -c 'import sys; from oak_decision_audio.web_display_node import main; main(sys.argv[1:])' "$@"
